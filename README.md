# Tibby

Satirical Slovak swipe-card game about power, bribes, and getting caught.

The design source of truth is [`docs/GDD.md`](docs/GDD.md).

## Develop

```sh
pnpm install
pnpm start
```

Then press `i` for the iOS Simulator or `a` for Android.
